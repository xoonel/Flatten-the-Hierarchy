#!/bin/bash

#Need to add var to define user input FROM path
#Need to add var to define user input TO path

#Does as expected. Before uncommenting and running, make sure to define paths. Will work on user input prompts for FROM and TO paths.  
#find ~/From_Path/Subpath '*.txt' -exec cp {} ~/To_Path/Subpath \;
